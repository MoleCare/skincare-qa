# Optional root containing sibling molecare-mcp, molecare-webapp, and skin-care-harness checkouts.
MOLECARE_ROOT=
# Optional direct path to the skin-care-harness Python package.
SKIN_CARE_HARNESS_PYTHON=
# Optional local inference endpoints.
OLLAMA_HOST=http://127.0.0.1:11434
HARNESS_HOST=http://127.0.0.1:8080
# HF_TOKEN=  # Only needed for authenticated Hugging Face operations; never commit a real token.
